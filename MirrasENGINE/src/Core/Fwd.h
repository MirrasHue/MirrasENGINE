#pragma once

namespace mirras
{
    class Event;
    class WindowResized;
    class WindowClosed;

    class Camera2D;
    class Texture;
    using Texture2D = Texture;

} // namespace mirras

namespace std::filesystem { }

namespace fs = std::filesystem;
