#pragma once

namespace mirras
{
    class Event;
    class WindowResized;
    class WindowClosed;

    class Camera2D;
    class Texture;
    using Texture2D = Texture;
    class Font;

} // namespace mirras

namespace std::filesystem { }

namespace fs = std::filesystem;
